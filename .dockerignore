# Build artifacts & deps (rebuilt inside the image)
**/node_modules
**/.nuxt
**/.nitro
**/.cache
**/.output
**/dist

# Secrets & local env
**/.env
**/.env.*

# VCS & CI (31MB+ of .git — never needed in the image)
.git
.github
fly.toml

# Test output & local scratch
test-results
playwright-report
e2e
dump.rdb
docker-compose.yml

# Logs & OS cruft
**/*.log*
**/.DS_Store
