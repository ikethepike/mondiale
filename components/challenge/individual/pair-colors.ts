/** Distinct wash per duel pair, so the higher-lower reveal's map highlight
 *  matches its cards. Shared by the gate that paints them and the reveal that
 *  reads them back. */
export const PAIR_COLORS = [
  'hsla(197.6, 51.2%, 41.8%, 0.55)', // soft-blue
  'hsla(29.7, 79.9%, 62%, 0.55)', // warm-sand
  'hsla(170.5, 24.7%, 55%, 0.6)', // soft-mint
]
